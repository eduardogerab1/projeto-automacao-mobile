[pytest]
# Arquivo ou pasta padrão para rodar
testpaths = 
    tests

# Opções padrão da linha de comando
addopts = 
    -s 
    -v

# Logging configuration
log_cli = true
log_cli_level = INFO
log_format = %(asctime)s - %(levelname)s - %(message)s
log_date_format = %Y-%m-%d %H:%M:%S

